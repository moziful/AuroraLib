import AddBookChecked from "./AddBookChecked";

export const metadata = {
  title: "Add Book",
  description: "Add a new book to the AuroraLib collection.",
};

export default function AddBookPage() {
  return <AddBookChecked />;
}
