import AddBookForm from "@/components/Dashboards/AddBookForm";

export const metadata = {
  title: "Add Book",
  description: "Add a new book to the AuroraLib collection.",
};

export default function AddBookPage() {
  return <AddBookForm />;
}
